#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;
void cutString(string s, int index){
    if(index > s.length());
    else{
        cout<<s.substr(index);
    }
}
int main() {
    
    return 0;
}